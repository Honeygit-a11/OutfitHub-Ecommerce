// let gstRate = null;
// let coupons =[];

// //save GST
// Router.post("/gst", (req,res)=>{
//     const {gst} = req.body;
//     if (!gst || isNaN(gst)){
//         return res.status(400).json({message:"Invalid GST value"});
//     }
//     gstRate = gst;
//     return res.json({message:"gst saved", gstRate});
// })

